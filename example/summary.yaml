# 14 species lumped into 1
lumped-species:
  - lumped: H2*
    actual: [H2(v1), H2(v10), H2(v11), H2(v12), H2(v13), H2(v14), H2(v2), H2(v3), H2(v4), H2(v5), H2(v6), H2(v7), H2(v8), H2(v9)]

# species which are never consumed
all-sources:
  - species: [H2(v0)]
# species which are only in balanced reactions
# or which have been explicitly marked as constant in the input file
constant-species:
  - species: []
# species which are only consumed
all-sinks:
  - species: [H2*]


unique-species:
  - count: 2
  - name: H2(v0)
    id: 0
  - name: H2*
    id: 1


reacion-summary:
  - H2(v0):
    - rate-based: 6
>>>>>>> 2e9956d (Working on improving the parameters object for ease of use)
      sources:
        - count: 6
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)

      balanced:
        - count: 0

      sinks:
        - count: 0

    - xsec-based: 0
      sources:
        - count: 0

      balanced:
        - count: 0

      sinks:
        - count: 0

  - H2*:
    - rate-based: 6
      sources:
        - count: 0

      balanced:
        - count: 0

      sinks:
        - count: 6
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)
        - H2* -> H2(v0)

    - xsec-based: 0
      sources:
        - count: 0

      balanced:
        - count: 0

      sinks:
        - count: 0

